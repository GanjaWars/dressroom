define(['backbone'], function(Backbone) {
    /**
     * @class SetModel
     * @extends Backbone.Model
     */
    var SetModel = Backbone.Model.extend(/** @lends SetModel */{

    });
    return SetModel;
});